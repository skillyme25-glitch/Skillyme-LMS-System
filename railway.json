{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install --include=dev && cd server && npm install --include=dev && cd .. && npx prisma generate && cd server && npm run build"

  },
  "deploy": {
    "startCommand": "npx prisma db push --schema=./prisma/schema.prisma --skip-generate && cd server && node dist/index.js",

    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
